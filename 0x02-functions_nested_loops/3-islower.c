#include "main.h"

/**
 * main - print alphabet islower.
 *
 * Return 1 if c islowercase
 * Return 0 otherwise
 */
int _islower(int c)
{
	if (c = 97; c < 122; c++)
	{
		_putchar(c);
		return (1);
	}
	else
		_putchar(c);
	return(0);
}
