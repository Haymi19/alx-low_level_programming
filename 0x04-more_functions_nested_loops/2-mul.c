#include "main.h"

/**
 * mul - prints the multible 0f a and b.
 *
 * @c: input number one.
 * @b: input number two.
 *
 * Return: Multible of a and b.
 */
int mul(int a, int b)
{
	return (a * b);
}

