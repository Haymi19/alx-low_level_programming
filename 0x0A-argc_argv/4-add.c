#include "main.h"

/**
 * add - Adds two positive integers and returns
 * the result.
 *
 * @a: integer one.
 * @b: integer two.
 *
 * Return: Add of integer one and integer two.
 */
int main(int argc, char *argv[])
{
	if (argv[a, b])
		return (a + b);
}
